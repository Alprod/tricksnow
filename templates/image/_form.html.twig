{{ form_start(form) }}
<div class="from-group">
    {{ form_row(form.title) }}
    <div class="input-group">
        {{ form_row(form.link, {'attr':{'class':'form-control', 'aria-label':'Upload'}}) }}
    </div>
    <button class="btn btn-outline-success mt-4">{{ button_label|default('Save') }}</button>
    <a class="btn btn-info mt-4" href="{{ path('figure_detail', {'id': image.figures.id}) }}">Retour figure</a>
</div>
{{ form_end(form) }}
