{{ form_start(form) }}
<div class="form-group">
    {{ form_row(form.title) }}
    {{ form_row(form.link) }}
    <button class="btn btn-outline-success mt-4">{{ button_label|default('Save') }}</button>
    <a class="btn btn-outline-info mt-4" href="{{ path('figure_detail', {'id': video.figures.id}) }}">Retour figure</a>
</div>
{{ form_end(form) }}
